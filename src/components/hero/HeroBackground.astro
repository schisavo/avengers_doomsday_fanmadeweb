

<div class="">
    <!-- CAPA 1: FONDO -->
    <div
        class="hero-background"
        data-parallax-background >
        <img
            src="/images/environments/paralax_1.png"
            alt=""/>
    </div>

    <!-- CAPA 2: PERSONAJE -->
    <div
        class="character-layer absolute inset-0 z-9 flex items-end justify-center pointer-events-none"
        data-parallax-character>
        <img
            src="/images/characters/villains/doom-paralax.png"
            alt="Doom"
            class="character-image" />
    </div>
</div>

<style>
    /* Capa 1 */
    .hero-background {
        position: absolute;
        z-index: 0;
        top: -5%;
        left: -5%;
        width: 110%;
        height: 110%;
        overflow: hidden;
        will-change: transform;
        border: 3px solid red;
    }
    .hero-background img {
        display: block;
        width: 100%;
        height: 100%;
        object-fit: cover;
        object-position: center;
    }
    /* Capa 2 */
    .character-layer {
        will-change: transform;
    }
    .character-image {
        width: clamp(320px, 55vw, 900px);
        height: auto;
        object-fit: contain;
        transform-origin: center bottom;
        will-change: transform;
    }
</style>