---
import type { CastMember } from "../../data/cast";

interface Props {
  member: CastMember;
}

const { member } = Astro.props;
---

<div
  class="cast-card group flex w-[9rem] shrink-0 cursor-pointer flex-col items-center sm:w-[11rem] md:w-[13rem] lg:w-[15rem]"
>
  <article
    class="relative h-[14rem] w-full overflow-hidden rounded-md sm:h-[16rem] md:h-[18rem] lg:h-[20rem]"
  >

    <!-- Imagen del actor -->
    <img
      src={member.actorImage}
      alt={member.actorName}
      loading="lazy"
      draggable="false"
      class="actor-img h-full w-full rounded-md object-cover shadow-[0_20px_40px_-20px_rgba(0,0,0,0.5)] transition-transform duration-500 ease-out group-hover:scale-[1.03]"
    />

    <!-- Imagen del personaje -->
    <img
      src={member.characterImage}
      alt={member.characterName}
      loading="lazy"
      draggable="false"
      class="character-img pointer-events-none absolute inset-0 h-full w-full object-contain opacity-0 translate-y-[20%] transition-all duration-500 ease-out group-hover:translate-y-0 group-hover:opacity-100"
    />

     <!-- Equipo / Team -->
    <span
      class="absolute left-2 bottom-1 z-20 rounded-md px-2.5 py-1 text-[0.65rem] font-bold uppercase tracking-wider text-white shadow-lg backdrop-blur-sm sm:left-3 sm:bottom-3 sm:px-3 sm:py-1.5 sm:text-xs"
      style={`background-color: ${member.teamColor};`}
    >
      {member.team}
    </span>

    <!-- Overlay sutil al hacer hover -->
    <div
      class="pointer-events-none absolute inset-0 bg-gradient-to-t from-black/30 via-transparent to-transparent opacity-0 transition-opacity duration-300 group-hover:opacity-100"
    >
    </div>
  </article>

  <!-- Nombre -->
  <p
    class="mt-2 w-full rounded px-2 py-1 text-center text-xs font-semibold text-white transition-all duration-300 sm:text-sm"
    style={`background-color: ${member.teamColor};`}
  >
    <span class="actor-name inline">
      {member.actorName}
    </span>

    <span class="character-name hidden">
      {member.characterName}
    </span>
  </p>
</div>

<style>
  /*
   * En dispositivos con hover real:
   * Actor → Personaje
   */
  @media (hover: hover) {
    .cast-card:hover .actor-name {
      display: none;
    }

    .cast-card:hover .character-name {
      display: inline;
    }
  }
</style>